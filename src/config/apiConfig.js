const URL_BASE = process.env.URL_BASE || "http://localhost:3000";

export const urlCreateUser = `${URL_BASE}/user/usuarios`;

export const urlLogin = `${URL_BASE}/user/login`;

export const urlGetProducts = `${URL_BASE}/product/products`;
